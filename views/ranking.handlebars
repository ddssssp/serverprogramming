<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <title>{{title}}</title>
  <!-- Bootstrap CSS -->
  <style>
    .horizontal-align {
      display: flex;
      align-items: center; /* 세로 축에서 가운데 정렬 */
      justify-content: center; /* 가로 축에서 가운데 정렬 */
    }
  </style>
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="container mt-5">
  <div class="horizontal-align mb-4">
    <h1>{{title}}</h1>
    <img src="img/onion.png" alt="양파"\>
  </div>

 <div class="d-flex justify-content-end mb-3">
    <a href="/" class="btn btn-outline-secondary">돌아가기</a>
  </div>

  <h3 class="mb-4 bg-success text-white">칭찬 양파 랭킹</h3>
  <table class="table">
    <thead>
      <tr>
        <th>순위</th>
        <th>작성자</th>
        <th>칭찬 글 수</th>
      </tr>
    </thead>
    <tbody>
      {{#each ranking_P}}
      <tr>
        <td>{{@index}}</td>
        <td>{{_id}}</td>
        <td>{{count}}</td>
      </tr>
      {{/each}}
    </tbody>
  </table>

 <div style="height: 10%;"></div>

  <h3 class="mb-4 bg-danger text-white">비난 양파 랭킹</h3>
  <table class="table">
    <thead>
      <tr>
        <th>순위</th>
        <th>작성자</th>
        <th>비난 글 수</th>
      </tr>
    </thead>
    <tbody>
      {{#each ranking_B}}
      <tr>
        <td>{{@index}}</td>
        <td>{{_id}}</td>
        <td>{{count}}</td>
      </tr>
      {{/each}}
    </tbody>
  </table>

  <!-- 다른 내용들 -->

  <!-- jQuery -->
  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <!-- Handlebars -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.6/handlebars.min.js"></script>
  <!-- Bootstrap JS and dependencies -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>

</html>
